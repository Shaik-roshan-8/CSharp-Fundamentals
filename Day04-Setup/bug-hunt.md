Bug here = Here the end is not included because we use i < end , so the correct format is i <= end.
                total += i;
Bug here = We used int it rounds up decimals so the correct format is using double data type .

Bug here = We used > that means it wont consider 90 , the correct format is >= 90 .